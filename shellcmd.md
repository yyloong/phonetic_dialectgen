### find
-type f 查找文件
-type d 查找目录
-name ".wav"

### ffmpeg

### shell 中 "$" 的使用
- 引用变量的值,如 file=10 用$file获取 10
- 命令替换 $(ls -1 sample) 会将命令输出为结果

### 计算操作
- (())

### for 语法
for x in xxx;do
    xxxx
done

### grep -c
